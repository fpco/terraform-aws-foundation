# Basic single node auto-scaling group (SNASG) example

### Getting started / Initial use

Set up a aws-env.config file in this directory.

Then run:

```
make ssh-key
make init 
make plan 
make apply
```
