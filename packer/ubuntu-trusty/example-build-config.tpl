{
    "source_ami": "${SOURCE_AMI}",
    "instance_type": "${INSTANCE_TYPE}",
    "vpc_id": "${VPC_ID}",
    "subnet_id": "${SUBNET_ID}",
    "region": "${REGION}",
    "copy_to_a": "${COPY_TO_A}",
    "copy_to_b": "${COPY_TO_B}",
    "git_rev": "${GIT_REV}",
    "build_id": "${BUILD_ID}",
    "description": "${DESC}",
    "os_version": "${OS_VERSION}",
    "release": "${RELEASE}"
}
